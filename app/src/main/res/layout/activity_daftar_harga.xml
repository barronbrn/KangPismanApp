<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_daftar_harga"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".view.activity.DaftarHargaActivity">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar_daftar_harga"
        android:layout_width="match_parent"
        android:layout_height="42dp"
        app:navigationIcon="@drawable/ic_arrow_back"
        app:title="Daftar Harga Sampah" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_semua_harga"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"
        app:layoutManager="androidx.recyclerview.widget.GridLayoutManager"
        app:spanCount="2"
        tools:listitem="@layout/item_harga" />

</LinearLayout>